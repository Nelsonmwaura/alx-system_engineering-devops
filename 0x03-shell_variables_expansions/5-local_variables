#!/bin/bash
echo "Local Variables:"; for var in $(set | grep "^[a-zA-Z_][a-zA-Z0-9_]*=" | cut -d= -f1); do echo "$var"; done; echo "Environment Variables:"; env | cut -d= -f1; echo "Functions:"; compgen -A function
